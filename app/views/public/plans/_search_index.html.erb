<%= form_tag(plans_path, method: "get") do %>
  <div class="d-flex justify-content-center">
    <%= text_field_tag(:search, params[:search], placeholder: "計画名・スポット名・県名を入力", class: "form-control") %>
    <%= submit_tag("検索", class: "btn btn-success") %>
  </div>
<% end %>

